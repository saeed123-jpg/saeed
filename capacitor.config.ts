import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.saeed.atlaspalestine",
  appName: "أطلس فلسطين التاريخي",
  webDir: "dist",
  bundledWebRuntime: false,
  android: {
    backgroundColor: "#18201e"
  }
};

export default config;
